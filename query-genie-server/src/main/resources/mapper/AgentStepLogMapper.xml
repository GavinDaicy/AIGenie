<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.genie.query.infrastructure.dao.mysql.mapper.AgentStepLogMapper">

    <insert id="insert" parameterType="com.genie.query.domain.agent.model.AgentStepLog">
        insert into agent_step_log (id, session_id, message_id, iteration, step_type, tool_name, content, duration_ms, created_at)
        values (#{id}, #{sessionId}, #{messageId}, #{iteration}, #{stepType}, #{toolName}, #{content}, #{durationMs}, #{createdAt})
    </insert>

    <select id="listBySessionId" resultType="com.genie.query.domain.agent.model.AgentStepLog">
        select * from agent_step_log
        where session_id = #{sessionId}
        order by created_at asc
    </select>

    <select id="listByMessageId" resultType="com.genie.query.domain.agent.model.AgentStepLog">
        select * from agent_step_log
        where message_id = #{messageId}
        order by created_at asc
    </select>

    <update id="updateMessageId">
        update agent_step_log
        set message_id = #{messageId}
        where session_id = #{sessionId} and message_id is null
    </update>

</mapper>
